{
  "version": 2,
  "name": "weightlifting-journal",
  "builds": [{ "src": "weightlifting-journal/package.json", "use": "@now/static-build", "config": {"distDir": "build"} }],
  "routes": [
    { "src": "/(.*)", "dest": "weightlifting-journal/$1"}
  ]
}